import {assert} from 'chai';
import * as helpers from '../lib/helpers';

describe('helpers', () => {
  it('should not be undefined', () => {
    assert.isDefined(helpers);
  });
  // TODO : more tests
});
